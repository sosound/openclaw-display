apiVersion: 1

providers:
  - name: 'Forum Dashboards'
    orgId: 1
    folder: 'Forum'
    folderUid: 'forum'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
